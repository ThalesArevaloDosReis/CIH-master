<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aluno.cih">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.activity_cadastro"
            android:label="@string/title_activity_cadastro" />
        <activity
            android:name=".View.activity_principal"
            android:label="@string/title_activity_principal" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:label="@string/title_activity_principal" />
        <activity android:name=".View.activity_publicacao" />
        <activity android:name=".View.EditarPerfil" />
        <activity android:name=".View.activity_config" />
        <activity android:name=".View.activity_termos" />
        <activity
            android:name=".View.activity_comentario"
            android:label="@string/title_activity_comentario"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".View.activity_novo_comentario"></activity>
    </application>

</manifest>